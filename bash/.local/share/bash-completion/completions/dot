# bash completion for dot
# Generated by dot completions bash. Do not edit by hand.

_dot_cmd_init() {
  local cur prev cword
  cur=${COMP_WORDS[COMP_CWORD]}
  prev=${COMP_WORDS[COMP_CWORD-1]}
  cword=$COMP_CWORD
  case $prev in
    --log)
      COMPREPLY=( $(compgen -f -- "$cur") )
      return
      ;;
  esac

  if [[ $cur == -* ]]; then
    COMPREPLY=( $(compgen -W '--confirm --noninteractive --interactive --force --host --log --help -h' -- "$cur") )
    return
  fi
}
_dot_cmd_install() {
  local cur prev cword
  cur=${COMP_WORDS[COMP_CWORD]}
  prev=${COMP_WORDS[COMP_CWORD-1]}
  cword=$COMP_CWORD
  if [[ $cur == -* ]]; then
    COMPREPLY=( $(compgen -W '--help -h' -- "$cur") )
    return
  fi
}
_dot_cmd_update() {
  local cur prev cword
  cur=${COMP_WORDS[COMP_CWORD]}
  prev=${COMP_WORDS[COMP_CWORD-1]}
  cword=$COMP_CWORD
  if [[ $cur == -* ]]; then
    COMPREPLY=( $(compgen -W '--pull --stow --app --check --check-all --help -h' -- "$cur") )
    return
  fi
}
_dot_cmd_stow() {
  local cur prev cword
  cur=${COMP_WORDS[COMP_CWORD]}
  prev=${COMP_WORDS[COMP_CWORD-1]}
  cword=$COMP_CWORD
  if [[ $cur == -* ]]; then
    COMPREPLY=( $(compgen -W '--public --private --help -h' -- "$cur") )
    return
  fi
}
_dot_cmd_omarchy_shell_config() {
  local cur prev cword
  cur=${COMP_WORDS[COMP_CWORD]}
  prev=${COMP_WORDS[COMP_CWORD-1]}
  cword=$COMP_CWORD
  if [[ $cur == -* ]]; then
    COMPREPLY=( $(compgen -W '--help -h' -- "$cur") )
    return
  fi
}
_dot_cmd_omarchy_plugin() {
  local cur prev cword subcommand
  cur=${COMP_WORDS[COMP_CWORD]}
  prev=${COMP_WORDS[COMP_CWORD-1]}
  cword=$COMP_CWORD
  subcommand=${COMP_WORDS[2]-}
  if (( cword > 2 )); then
    case $subcommand in
      add)
        _dot_cmd_omarchy_plugin_add
        return
        ;;
      update)
        _dot_cmd_omarchy_plugin_update
        return
        ;;
      remove)
        _dot_cmd_omarchy_plugin_remove
        return
        ;;
    esac
  fi
  if [[ $cur == -* ]]; then
    COMPREPLY=( $(compgen -W '--help -h' -- "$cur") )
    return
  fi
  if (( cword == 2 )); then
    COMPREPLY=( $(compgen -W '--help -h add update remove' -- "$cur") )
    return
  fi
  COMPREPLY=( $(compgen -W 'add update remove' -- "$cur") )
}
_dot_cmd_omarchy_plugin_add() {
  local cur prev cword
  cur=${COMP_WORDS[COMP_CWORD]}
  prev=${COMP_WORDS[COMP_CWORD-1]}
  cword=$COMP_CWORD
  case $prev in
    --section)
      COMPREPLY=( $(compgen -W 'left center right' -- "$cur") )
      return
      ;;
  esac

  if [[ $cur == -* ]]; then
    COMPREPLY=( $(compgen -W '--section --before --after --help -h' -- "$cur") )
    return
  fi
  if (( cword == 5 )); then
    COMPREPLY=( $(compgen -f -- "$cur") )
    return
  fi
}
_dot_cmd_omarchy_plugin_update() {
  local cur prev cword
  cur=${COMP_WORDS[COMP_CWORD]}
  prev=${COMP_WORDS[COMP_CWORD-1]}
  cword=$COMP_CWORD
  if [[ $cur == -* ]]; then
    COMPREPLY=( $(compgen -W '--yes --help -h' -- "$cur") )
    return
  fi
}
_dot_cmd_omarchy_plugin_remove() {
  local cur prev cword
  cur=${COMP_WORDS[COMP_CWORD]}
  prev=${COMP_WORDS[COMP_CWORD-1]}
  cword=$COMP_CWORD
  if [[ $cur == -* ]]; then
    COMPREPLY=( $(compgen -W '--yes --no-commit-offer --help -h' -- "$cur") )
    return
  fi
}
_dot_cmd_firewall() {
  local cur prev cword
  cur=${COMP_WORDS[COMP_CWORD]}
  prev=${COMP_WORDS[COMP_CWORD-1]}
  cword=$COMP_CWORD
  if [[ $cur == -* ]]; then
    COMPREPLY=( $(compgen -W '--help -h' -- "$cur") )
    return
  fi
}
_dot_cmd_doctor() {
  local cur prev cword
  cur=${COMP_WORDS[COMP_CWORD]}
  prev=${COMP_WORDS[COMP_CWORD-1]}
  cword=$COMP_CWORD
  if [[ $cur == -* ]]; then
    COMPREPLY=( $(compgen -W '--open-opencode --help -h' -- "$cur") )
    return
  fi
}
_dot_cmd_clean() {
  local cur prev cword
  cur=${COMP_WORDS[COMP_CWORD]}
  prev=${COMP_WORDS[COMP_CWORD-1]}
  cword=$COMP_CWORD
  if [[ $cur == -* ]]; then
    COMPREPLY=( $(compgen -W '--help -h' -- "$cur") )
    return
  fi
}
_dot_cmd_git_diff() {
  local cur prev cword
  cur=${COMP_WORDS[COMP_CWORD]}
  prev=${COMP_WORDS[COMP_CWORD-1]}
  cword=$COMP_CWORD
  if [[ $cur == -* ]]; then
    COMPREPLY=( $(compgen -W '--no-fetch --raw --bar-json --panel-json --list-changed --list-all --help -h' -- "$cur") )
    return
  fi
}
_dot_cmd_git_commit() {
  local cur prev cword
  cur=${COMP_WORDS[COMP_CWORD]}
  prev=${COMP_WORDS[COMP_CWORD-1]}
  cword=$COMP_CWORD
  case $prev in
    --path)
      COMPREPLY=( $(compgen -f -- "$cur") )
      return
      ;;
  esac

  if [[ $cur == -* ]]; then
    COMPREPLY=( $(compgen -W '--message -m --path --amend --push --dry-run --help -h' -- "$cur") )
    return
  fi
}
_dot_cmd_git_notifications() {
  local cur prev cword
  cur=${COMP_WORDS[COMP_CWORD]}
  prev=${COMP_WORDS[COMP_CWORD-1]}
  cword=$COMP_CWORD
  if [[ $cur == -* ]]; then
    COMPREPLY=( $(compgen -W '--raw --bar-json --list-threads --bar-filter --all --participating --since --mark-read --mark-bot-read --dry-run --mark-done --ignore --unignore --help -h' -- "$cur") )
    return
  fi
}
_dot_cmd_agents_sync() {
  local cur prev cword
  cur=${COMP_WORDS[COMP_CWORD]}
  prev=${COMP_WORDS[COMP_CWORD-1]}
  cword=$COMP_CWORD
  if [[ $cur == -* ]]; then
    COMPREPLY=( $(compgen -W '--help -h' -- "$cur") )
    return
  fi
}
_dot_cmd_mcp_sync() {
  local cur prev cword
  cur=${COMP_WORDS[COMP_CWORD]}
  prev=${COMP_WORDS[COMP_CWORD-1]}
  cword=$COMP_CWORD
  if [[ $cur == -* ]]; then
    COMPREPLY=( $(compgen -W '--help -h' -- "$cur") )
    return
  fi
}
_dot_cmd_notes_capture_sync() {
  local cur prev cword
  cur=${COMP_WORDS[COMP_CWORD]}
  prev=${COMP_WORDS[COMP_CWORD-1]}
  cword=$COMP_CWORD
  if [[ $cur == -* ]]; then
    COMPREPLY=( $(compgen -W '--help -h' -- "$cur") )
    return
  fi
}
_dot_cmd_is_agent() {
  local cur prev cword
  cur=${COMP_WORDS[COMP_CWORD]}
  prev=${COMP_WORDS[COMP_CWORD-1]}
  cword=$COMP_CWORD
  if [[ $cur == -* ]]; then
    COMPREPLY=( $(compgen -W '--quiet -q --json --help -h' -- "$cur") )
    return
  fi
}
_dot_cmd_setup_public_repo() {
  local cur prev cword
  cur=${COMP_WORDS[COMP_CWORD]}
  prev=${COMP_WORDS[COMP_CWORD-1]}
  cword=$COMP_CWORD
  if [[ $cur == -* ]]; then
    COMPREPLY=( $(compgen -W '--help -h' -- "$cur") )
    return
  fi
}
_dot_cmd_setup_private_repo() {
  local cur prev cword
  cur=${COMP_WORDS[COMP_CWORD]}
  prev=${COMP_WORDS[COMP_CWORD-1]}
  cword=$COMP_CWORD
  if [[ $cur == -* ]]; then
    COMPREPLY=( $(compgen -W '--help -h' -- "$cur") )
    return
  fi
}
_dot_cmd_private_pkg_publish() {
  local cur prev cword
  cur=${COMP_WORDS[COMP_CWORD]}
  prev=${COMP_WORDS[COMP_CWORD-1]}
  cword=$COMP_CWORD
  if [[ $cur == -* ]]; then
    COMPREPLY=( $(compgen -W '--no-git --skip-build --install --help -h' -- "$cur") )
    return
  fi
}
_dot_cmd_skill_updates() {
  local cur prev cword
  cur=${COMP_WORDS[COMP_CWORD]}
  prev=${COMP_WORDS[COMP_CWORD-1]}
  cword=$COMP_CWORD
  if [[ $cur == -* ]]; then
    COMPREPLY=( $(compgen -W '--check --update --json --skill --no-commit --skip-review --help -h' -- "$cur") )
    return
  fi
}
_dot_cmd_skill_check() {
  local cur prev cword
  cur=${COMP_WORDS[COMP_CWORD]}
  prev=${COMP_WORDS[COMP_CWORD-1]}
  cword=$COMP_CWORD
  if [[ $cur == -* ]]; then
    COMPREPLY=( $(compgen -W '--open-opencode --diff-origin --skill --help -h' -- "$cur") )
    return
  fi
}
_dot_cmd_skill_updates_agent() {
  local cur prev cword
  cur=${COMP_WORDS[COMP_CWORD]}
  prev=${COMP_WORDS[COMP_CWORD-1]}
  cword=$COMP_CWORD
  case $prev in
    --config)
      COMPREPLY=( $(compgen -f -- "$cur") )
      return
      ;;
    --skills-dir)
      COMPREPLY=( $(compgen -f -- "$cur") )
      return
      ;;
  esac

  if [[ $cur == -* ]]; then
    COMPREPLY=( $(compgen -W '--config --run-id --skills-dir --help -h' -- "$cur") )
    return
  fi
  if (( cword == 2 )); then
    COMPREPLY=( $(compgen -W 'github device' -- "$cur") )
    return
  fi
}
_dot_cmd_completions() {
  local cur prev cword
  cur=${COMP_WORDS[COMP_CWORD]}
  prev=${COMP_WORDS[COMP_CWORD-1]}
  cword=$COMP_CWORD
  if [[ $cur == -* ]]; then
    COMPREPLY=( $(compgen -W '--stdout --help -h' -- "$cur") )
    return
  fi
  if (( cword == 2 )); then
    COMPREPLY=( $(compgen -W 'bash fish zsh' -- "$cur") )
    return
  fi
}
_dot_cmd_launch_floating_webapp() {
  local cur prev cword
  cur=${COMP_WORDS[COMP_CWORD]}
  prev=${COMP_WORDS[COMP_CWORD-1]}
  cword=$COMP_CWORD
  if [[ $cur == -* ]]; then
    COMPREPLY=( $(compgen -W '--monitor --workspace --width --height --right-margin --bottom-margin --address --help -h' -- "$cur") )
    return
  fi
}
_dot_cmd_workspace_relayout() {
  local cur prev cword
  cur=${COMP_WORDS[COMP_CWORD]}
  prev=${COMP_WORDS[COMP_CWORD-1]}
  cword=$COMP_CWORD
  if [[ $cur == -* ]]; then
    COMPREPLY=( $(compgen -W '--edit --help -h' -- "$cur") )
    return
  fi
}
_dot_cmd_workspace_capture() {
  local cur prev cword
  cur=${COMP_WORDS[COMP_CWORD]}
  prev=${COMP_WORDS[COMP_CWORD-1]}
  cword=$COMP_CWORD
  case $prev in
    --output)
      COMPREPLY=( $(compgen -f -- "$cur") )
      return
      ;;
    --state-dir)
      COMPREPLY=( $(compgen -f -- "$cur") )
      return
      ;;
  esac

  if [[ $cur == -* ]]; then
    COMPREPLY=( $(compgen -W '--current-workspace --output --state-dir --help -h' -- "$cur") )
    return
  fi
}
_dot_cmd_workspace_restore() {
  local cur prev cword
  cur=${COMP_WORDS[COMP_CWORD]}
  prev=${COMP_WORDS[COMP_CWORD-1]}
  cword=$COMP_CWORD
  case $prev in
    --file)
      COMPREPLY=( $(compgen -f -- "$cur") )
      return
      ;;
    --state-dir)
      COMPREPLY=( $(compgen -f -- "$cur") )
      return
      ;;
  esac

  if [[ $cur == -* ]]; then
    COMPREPLY=( $(compgen -W '--dry-run --file --state-dir --no-launch --no-move --help -h' -- "$cur") )
    return
  fi
}
_dot_cmd_usage() {
  local cur prev cword
  cur=${COMP_WORDS[COMP_CWORD]}
  prev=${COMP_WORDS[COMP_CWORD-1]}
  cword=$COMP_CWORD
  case $prev in
    --format)
      COMPREPLY=( $(compgen -W 'text json agent-context' -- "$cur") )
      return
      ;;
    --root)
      COMPREPLY=( $(compgen -f -- "$cur") )
      return
      ;;
  esac

  if [[ $cur == -* ]]; then
    COMPREPLY=( $(compgen -W '--days --format --root --history --apply --help -h' -- "$cur") )
    return
  fi
}
_dot_cmd_help() {
  local cur prev cword
  cur=${COMP_WORDS[COMP_CWORD]}
  prev=${COMP_WORDS[COMP_CWORD-1]}
  cword=$COMP_CWORD
  if [[ $cur == -* ]]; then
    COMPREPLY=( $(compgen -W '--help -h' -- "$cur") )
    return
  fi
}
_dot() {
  local cur cword command
  COMPREPLY=()
  cur=${COMP_WORDS[COMP_CWORD]}
  cword=$COMP_CWORD
  command=${COMP_WORDS[1]-}

  if (( cword == 1 )); then
    COMPREPLY=( $(compgen -W 'init install update up stow omarchy-shell-config omarchy-plugin firewall doctor clean git-diff diff git-commit git-notifications agents-sync mcp-sync notes-capture-sync is-agent setup-public-repo setup-private-repo private-pkg-publish skill-updates skill-check skill-updates-agent completions launch-floating-webapp workspace-relayout workspace-capture workspace-restore usage help -h --help' -- "$cur") )
    return
  fi

  case $command in
    init)
      _dot_cmd_init
      ;;
    install)
      _dot_cmd_install
      ;;
    update|up)
      _dot_cmd_update
      ;;
    stow)
      _dot_cmd_stow
      ;;
    omarchy-shell-config)
      _dot_cmd_omarchy_shell_config
      ;;
    omarchy-plugin)
      _dot_cmd_omarchy_plugin
      ;;
    firewall)
      _dot_cmd_firewall
      ;;
    doctor)
      _dot_cmd_doctor
      ;;
    clean)
      _dot_cmd_clean
      ;;
    git-diff|diff)
      _dot_cmd_git_diff
      ;;
    git-commit)
      _dot_cmd_git_commit
      ;;
    git-notifications)
      _dot_cmd_git_notifications
      ;;
    agents-sync)
      _dot_cmd_agents_sync
      ;;
    mcp-sync)
      _dot_cmd_mcp_sync
      ;;
    notes-capture-sync)
      _dot_cmd_notes_capture_sync
      ;;
    is-agent)
      _dot_cmd_is_agent
      ;;
    setup-public-repo)
      _dot_cmd_setup_public_repo
      ;;
    setup-private-repo)
      _dot_cmd_setup_private_repo
      ;;
    private-pkg-publish)
      _dot_cmd_private_pkg_publish
      ;;
    skill-updates)
      _dot_cmd_skill_updates
      ;;
    skill-check)
      _dot_cmd_skill_check
      ;;
    skill-updates-agent)
      _dot_cmd_skill_updates_agent
      ;;
    completions)
      _dot_cmd_completions
      ;;
    launch-floating-webapp)
      _dot_cmd_launch_floating_webapp
      ;;
    workspace-relayout)
      _dot_cmd_workspace_relayout
      ;;
    workspace-capture)
      _dot_cmd_workspace_capture
      ;;
    workspace-restore)
      _dot_cmd_workspace_restore
      ;;
    usage)
      _dot_cmd_usage
      ;;
    help)
      _dot_cmd_help
      ;;
  esac
}

complete -F _dot dot

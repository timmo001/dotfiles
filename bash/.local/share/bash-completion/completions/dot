# bash completion for dot
# Generated by dot completions bash. Do not edit by hand.

_dot_cmd_dashboard() {
  local cur prev cword
  cur=${COMP_WORDS[COMP_CWORD]}
  prev=${COMP_WORDS[COMP_CWORD-1]}
  cword=$COMP_CWORD
  if [[ $cur == -* ]]; then
    COMPREPLY=( $(compgen -W '--help -h' -- "$cur") )
    return
  fi
}
_dot_cmd_init() {
  local cur prev cword
  cur=${COMP_WORDS[COMP_CWORD]}
  prev=${COMP_WORDS[COMP_CWORD-1]}
  cword=$COMP_CWORD
  case $prev in
    --host)
      COMPREPLY=( $(compgen -W 'desktop laptop' -- "$cur") )
      return
      ;;
    --log)
      COMPREPLY=( $(compgen -f -- "$cur") )
      return
      ;;
  esac

  if [[ $cur == -* ]]; then
    COMPREPLY=( $(compgen -W '--confirm --noninteractive --interactive --force --host --log --branch --bootstrap-branch --help -h' -- "$cur") )
    return
  fi
}
_dot_cmd_install() {
  local cur prev cword
  cur=${COMP_WORDS[COMP_CWORD]}
  prev=${COMP_WORDS[COMP_CWORD-1]}
  cword=$COMP_CWORD
  if [[ $cur == -* ]]; then
    COMPREPLY=( $(compgen -W '--help -h' -- "$cur") )
    return
  fi
}
_dot_cmd_update() {
  local cur prev cword
  cur=${COMP_WORDS[COMP_CWORD]}
  prev=${COMP_WORDS[COMP_CWORD-1]}
  cword=$COMP_CWORD
  if [[ $cur == -* ]]; then
    COMPREPLY=( $(compgen -W '--pull --stow --tui --check --check-all --help -h' -- "$cur") )
    return
  fi
}
_dot_cmd_stow() {
  local cur prev cword
  cur=${COMP_WORDS[COMP_CWORD]}
  prev=${COMP_WORDS[COMP_CWORD-1]}
  cword=$COMP_CWORD
  if [[ $cur == -* ]]; then
    COMPREPLY=( $(compgen -W '--public --private --help -h' -- "$cur") )
    return
  fi
}
_dot_cmd_doctor() {
  local cur prev cword
  cur=${COMP_WORDS[COMP_CWORD]}
  prev=${COMP_WORDS[COMP_CWORD-1]}
  cword=$COMP_CWORD
  if [[ $cur == -* ]]; then
    COMPREPLY=( $(compgen -W '--open-opencode --help -h' -- "$cur") )
    return
  fi
}
_dot_cmd_clean() {
  local cur prev cword
  cur=${COMP_WORDS[COMP_CWORD]}
  prev=${COMP_WORDS[COMP_CWORD-1]}
  cword=$COMP_CWORD
  if [[ $cur == -* ]]; then
    COMPREPLY=( $(compgen -W '--help -h' -- "$cur") )
    return
  fi
}
_dot_cmd_git_diff() {
  local cur prev cword
  cur=${COMP_WORDS[COMP_CWORD]}
  prev=${COMP_WORDS[COMP_CWORD-1]}
  cword=$COMP_CWORD
  case $prev in
    --tab)
      COMPREPLY=( $(compgen -W 'changed other unchanged' -- "$cur") )
      return
      ;;
  esac

  if [[ $cur == -* ]]; then
    COMPREPLY=( $(compgen -W '--no-fetch --tab --raw --bar-json --list-changed --list-all --help -h' -- "$cur") )
    return
  fi
}
_dot_cmd_git_status() {
  local cur prev cword
  cur=${COMP_WORDS[COMP_CWORD]}
  prev=${COMP_WORDS[COMP_CWORD-1]}
  cword=$COMP_CWORD
  if [[ $cur == -* ]]; then
    COMPREPLY=( $(compgen -W '--diff --branch-diff --since --help -h' -- "$cur") )
    return
  fi
}
_dot_cmd_git_commit() {
  local cur prev cword
  cur=${COMP_WORDS[COMP_CWORD]}
  prev=${COMP_WORDS[COMP_CWORD-1]}
  cword=$COMP_CWORD
  case $prev in
    --path)
      COMPREPLY=( $(compgen -f -- "$cur") )
      return
      ;;
  esac

  if [[ $cur == -* ]]; then
    COMPREPLY=( $(compgen -W '--message -m --path --push --dry-run --help -h' -- "$cur") )
    return
  fi
}
_dot_cmd_git_log() {
  local cur prev cword
  cur=${COMP_WORDS[COMP_CWORD]}
  prev=${COMP_WORDS[COMP_CWORD-1]}
  cword=$COMP_CWORD
  if [[ $cur == -* ]]; then
    COMPREPLY=( $(compgen -W '--raw --help -h' -- "$cur") )
    return
  fi
}
_dot_cmd_git_workflows() {
  local cur prev cword
  cur=${COMP_WORDS[COMP_CWORD]}
  prev=${COMP_WORDS[COMP_CWORD-1]}
  cword=$COMP_CWORD
  if [[ $cur == -* ]]; then
    COMPREPLY=( $(compgen -W '--since --raw --bar-json --list-repos --list-runs --help -h' -- "$cur") )
    return
  fi
}
_dot_cmd_git_notifications() {
  local cur prev cword
  cur=${COMP_WORDS[COMP_CWORD]}
  prev=${COMP_WORDS[COMP_CWORD-1]}
  cword=$COMP_CWORD
  if [[ $cur == -* ]]; then
    COMPREPLY=( $(compgen -W '--raw --bar-json --list-threads --bar-filter --all --participating --since --mark-read --mark-bot-read --dry-run --mark-done --ignore --unignore --help -h' -- "$cur") )
    return
  fi
}
_dot_cmd_notes() {
  local cur prev cword subcommand
  cur=${COMP_WORDS[COMP_CWORD]}
  prev=${COMP_WORDS[COMP_CWORD-1]}
  cword=$COMP_CWORD
  subcommand=${COMP_WORDS[2]-}
  if (( cword > 2 )); then
    case $subcommand in
      root)
        _dot_cmd_notes_root
        return
        ;;
      context)
        _dot_cmd_notes_context
        return
        ;;
      list)
        _dot_cmd_notes_list
        return
        ;;
    esac
  fi
  if [[ $cur == -* ]]; then
    COMPREPLY=( $(compgen -W '--all --help -h' -- "$cur") )
    return
  fi
  if (( cword == 2 )); then
    COMPREPLY=( $(compgen -W '--all --help -h root context list' -- "$cur") )
    return
  fi
  COMPREPLY=( $(compgen -W 'root context list' -- "$cur") )
}
_dot_cmd_notes_root() {
  local cur prev cword
  cur=${COMP_WORDS[COMP_CWORD]}
  prev=${COMP_WORDS[COMP_CWORD-1]}
  cword=$COMP_CWORD
  if [[ $cur == -* ]]; then
    COMPREPLY=( $(compgen -W '--repo-notes' -- "$cur") )
    return
  fi
}
_dot_cmd_notes_context() {
  local cur prev cword
  cur=${COMP_WORDS[COMP_CWORD]}
  prev=${COMP_WORDS[COMP_CWORD-1]}
  cword=$COMP_CWORD
  if [[ $cur == -* ]]; then
    COMPREPLY=( $(compgen -W '--command' -- "$cur") )
    return
  fi
}
_dot_cmd_notes_list() {
  local cur prev cword
  cur=${COMP_WORDS[COMP_CWORD]}
  prev=${COMP_WORDS[COMP_CWORD-1]}
  cword=$COMP_CWORD
  case $prev in
    --format)
      COMPREPLY=( $(compgen -W 'labels json' -- "$cur") )
      return
      ;;
  esac

  if [[ $cur == -* ]]; then
    COMPREPLY=( $(compgen -W '--all --format' -- "$cur") )
    return
  fi
}
_dot_cmd_handoffs() {
  local cur prev cword
  cur=${COMP_WORDS[COMP_CWORD]}
  prev=${COMP_WORDS[COMP_CWORD-1]}
  cword=$COMP_CWORD
  if [[ $cur == -* ]]; then
    COMPREPLY=( $(compgen -W '--all --list --help -h' -- "$cur") )
    return
  fi
}
_dot_cmd_note() {
  local cur prev cword subcommand
  cur=${COMP_WORDS[COMP_CWORD]}
  prev=${COMP_WORDS[COMP_CWORD-1]}
  cword=$COMP_CWORD
  subcommand=${COMP_WORDS[2]-}
  if (( cword > 2 )); then
    case $subcommand in
      read)
        _dot_cmd_note_read
        return
        ;;
      write)
        _dot_cmd_note_write
        return
        ;;
      delete)
        _dot_cmd_note_delete
        return
        ;;
    esac
  fi
  if [[ $cur == -* ]]; then
    COMPREPLY=( $(compgen -W '--help -h' -- "$cur") )
    return
  fi
  if (( cword == 2 )); then
    COMPREPLY=( $(compgen -W '--help -h read write delete' -- "$cur") )
    return
  fi
  COMPREPLY=( $(compgen -W 'read write delete' -- "$cur") )
}
_dot_cmd_note_read() {
  local cur prev cword
  cur=${COMP_WORDS[COMP_CWORD]}
  prev=${COMP_WORDS[COMP_CWORD-1]}
  cword=$COMP_CWORD
  case $prev in
    --path)
      COMPREPLY=( $(compgen -f -- "$cur") )
      return
      ;;
  esac

  if [[ $cur == -* ]]; then
    COMPREPLY=( $(compgen -W '--path' -- "$cur") )
    return
  fi
}
_dot_cmd_note_write() {
  local cur prev cword
  cur=${COMP_WORDS[COMP_CWORD]}
  prev=${COMP_WORDS[COMP_CWORD-1]}
  cword=$COMP_CWORD
  case $prev in
    --path)
      COMPREPLY=( $(compgen -f -- "$cur") )
      return
      ;;
  esac

  if [[ $cur == -* ]]; then
    COMPREPLY=( $(compgen -W '--path --stdin' -- "$cur") )
    return
  fi
}
_dot_cmd_note_delete() {
  local cur prev cword
  cur=${COMP_WORDS[COMP_CWORD]}
  prev=${COMP_WORDS[COMP_CWORD-1]}
  cword=$COMP_CWORD
  case $prev in
    --path)
      COMPREPLY=( $(compgen -f -- "$cur") )
      return
      ;;
  esac

  if [[ $cur == -* ]]; then
    COMPREPLY=( $(compgen -W '--path' -- "$cur") )
    return
  fi
}
_dot_cmd_agents_sync() {
  local cur prev cword
  cur=${COMP_WORDS[COMP_CWORD]}
  prev=${COMP_WORDS[COMP_CWORD-1]}
  cword=$COMP_CWORD
  if [[ $cur == -* ]]; then
    COMPREPLY=( $(compgen -W '--help -h' -- "$cur") )
    return
  fi
}
_dot_cmd_opencode_debug() {
  local cur prev cword
  cur=${COMP_WORDS[COMP_CWORD]}
  prev=${COMP_WORDS[COMP_CWORD-1]}
  cword=$COMP_CWORD
  if [[ $cur == -* ]]; then
    COMPREPLY=( $(compgen -W '--agent --help -h' -- "$cur") )
    return
  fi
}
_dot_cmd_setup_private_repo() {
  local cur prev cword
  cur=${COMP_WORDS[COMP_CWORD]}
  prev=${COMP_WORDS[COMP_CWORD-1]}
  cword=$COMP_CWORD
  if [[ $cur == -* ]]; then
    COMPREPLY=( $(compgen -W '--help -h' -- "$cur") )
    return
  fi
}
_dot_cmd_private_pkg_publish() {
  local cur prev cword
  cur=${COMP_WORDS[COMP_CWORD]}
  prev=${COMP_WORDS[COMP_CWORD-1]}
  cword=$COMP_CWORD
  if [[ $cur == -* ]]; then
    COMPREPLY=( $(compgen -W '--no-git --skip-build --install --help -h' -- "$cur") )
    return
  fi
}
_dot_cmd_skill_updates() {
  local cur prev cword
  cur=${COMP_WORDS[COMP_CWORD]}
  prev=${COMP_WORDS[COMP_CWORD-1]}
  cword=$COMP_CWORD
  if [[ $cur == -* ]]; then
    COMPREPLY=( $(compgen -W '--check --update --skip-review --help -h' -- "$cur") )
    return
  fi
}
_dot_cmd_skill_check() {
  local cur prev cword
  cur=${COMP_WORDS[COMP_CWORD]}
  prev=${COMP_WORDS[COMP_CWORD-1]}
  cword=$COMP_CWORD
  if [[ $cur == -* ]]; then
    COMPREPLY=( $(compgen -W '--open-opencode --diff-origin --help -h' -- "$cur") )
    return
  fi
}
_dot_cmd_completions() {
  local cur prev cword
  cur=${COMP_WORDS[COMP_CWORD]}
  prev=${COMP_WORDS[COMP_CWORD-1]}
  cword=$COMP_CWORD
  if [[ $cur == -* ]]; then
    COMPREPLY=( $(compgen -W '--stdout --help -h' -- "$cur") )
    return
  fi
  if (( cword == 2 )); then
    COMPREPLY=( $(compgen -W 'bash fish zsh' -- "$cur") )
    return
  fi
}
_dot_cmd_omarchy() {
  local cur prev cword
  cur=${COMP_WORDS[COMP_CWORD]}
  prev=${COMP_WORDS[COMP_CWORD-1]}
  cword=$COMP_CWORD
  if [[ $cur == -* ]]; then
    COMPREPLY=( $(compgen -W '--help -h' -- "$cur") )
    return
  fi
  if (( cword >= 2 )); then
    COMPREPLY=( $(compgen -W 'theme font toggle capture system launch refresh restart install remove packages share reminder setup snapshot brightness power' -- "$cur") )
    return
  fi
}
_dot_cmd_help() {
  local cur prev cword
  cur=${COMP_WORDS[COMP_CWORD]}
  prev=${COMP_WORDS[COMP_CWORD-1]}
  cword=$COMP_CWORD
  if [[ $cur == -* ]]; then
    COMPREPLY=( $(compgen -W '--help -h' -- "$cur") )
    return
  fi
}
_dot() {
  local cur cword command
  COMPREPLY=()
  cur=${COMP_WORDS[COMP_CWORD]}
  cword=$COMP_CWORD
  command=${COMP_WORDS[1]-}

  if (( cword == 1 )); then
    COMPREPLY=( $(compgen -W 'dashboard init install update up stow doctor clean git-diff diff git-status git-commit git-log git-workflows git-notifications notes handoffs handoff note agents-sync opencode-debug setup-private-repo private-pkg-publish skill-updates skill-check completions omarchy help -h --help' -- "$cur") )
    return
  fi

  case $command in
    dashboard)
      _dot_cmd_dashboard
      ;;
    init)
      _dot_cmd_init
      ;;
    install)
      _dot_cmd_install
      ;;
    update|up)
      _dot_cmd_update
      ;;
    stow)
      _dot_cmd_stow
      ;;
    doctor)
      _dot_cmd_doctor
      ;;
    clean)
      _dot_cmd_clean
      ;;
    git-diff|diff)
      _dot_cmd_git_diff
      ;;
    git-status)
      _dot_cmd_git_status
      ;;
    git-commit)
      _dot_cmd_git_commit
      ;;
    git-log)
      _dot_cmd_git_log
      ;;
    git-workflows)
      _dot_cmd_git_workflows
      ;;
    git-notifications)
      _dot_cmd_git_notifications
      ;;
    notes)
      _dot_cmd_notes
      ;;
    handoffs|handoff)
      _dot_cmd_handoffs
      ;;
    note)
      _dot_cmd_note
      ;;
    agents-sync)
      _dot_cmd_agents_sync
      ;;
    opencode-debug)
      _dot_cmd_opencode_debug
      ;;
    setup-private-repo)
      _dot_cmd_setup_private_repo
      ;;
    private-pkg-publish)
      _dot_cmd_private_pkg_publish
      ;;
    skill-updates)
      _dot_cmd_skill_updates
      ;;
    skill-check)
      _dot_cmd_skill_check
      ;;
    completions)
      _dot_cmd_completions
      ;;
    omarchy)
      _dot_cmd_omarchy
      ;;
    help)
      _dot_cmd_help
      ;;
  esac
}

complete -F _dot dot

# bash completion for dot
# Generated by dot completions bash. Do not edit by hand.

_dot_cmd_dashboard() {
  local cur prev cword
  cur=${COMP_WORDS[COMP_CWORD]}
  prev=${COMP_WORDS[COMP_CWORD-1]}
  cword=$COMP_CWORD
  if [[ $cur == -* ]]; then
    COMPREPLY=( $(compgen -W '--help -h' -- "$cur") )
    return
  fi
}
_dot_cmd_init() {
  local cur prev cword
  cur=${COMP_WORDS[COMP_CWORD]}
  prev=${COMP_WORDS[COMP_CWORD-1]}
  cword=$COMP_CWORD
  case $prev in
    --log)
      COMPREPLY=( $(compgen -f -- "$cur") )
      return
      ;;
  esac

  if [[ $cur == -* ]]; then
    COMPREPLY=( $(compgen -W '--confirm --noninteractive --interactive --force --host --log --branch --help -h' -- "$cur") )
    return
  fi
}
_dot_cmd_install() {
  local cur prev cword
  cur=${COMP_WORDS[COMP_CWORD]}
  prev=${COMP_WORDS[COMP_CWORD-1]}
  cword=$COMP_CWORD
  if [[ $cur == -* ]]; then
    COMPREPLY=( $(compgen -W '--help -h' -- "$cur") )
    return
  fi
}
_dot_cmd_update() {
  local cur prev cword
  cur=${COMP_WORDS[COMP_CWORD]}
  prev=${COMP_WORDS[COMP_CWORD-1]}
  cword=$COMP_CWORD
  if [[ $cur == -* ]]; then
    COMPREPLY=( $(compgen -W '--pull --stow --app --check --check-all --help -h' -- "$cur") )
    return
  fi
}
_dot_cmd_stow() {
  local cur prev cword
  cur=${COMP_WORDS[COMP_CWORD]}
  prev=${COMP_WORDS[COMP_CWORD-1]}
  cword=$COMP_CWORD
  if [[ $cur == -* ]]; then
    COMPREPLY=( $(compgen -W '--public --private --help -h' -- "$cur") )
    return
  fi
}
_dot_cmd_firewall() {
  local cur prev cword
  cur=${COMP_WORDS[COMP_CWORD]}
  prev=${COMP_WORDS[COMP_CWORD-1]}
  cword=$COMP_CWORD
  if [[ $cur == -* ]]; then
    COMPREPLY=( $(compgen -W '--help -h' -- "$cur") )
    return
  fi
}
_dot_cmd_doctor() {
  local cur prev cword
  cur=${COMP_WORDS[COMP_CWORD]}
  prev=${COMP_WORDS[COMP_CWORD-1]}
  cword=$COMP_CWORD
  if [[ $cur == -* ]]; then
    COMPREPLY=( $(compgen -W '--open-opencode --help -h' -- "$cur") )
    return
  fi
}
_dot_cmd_clean() {
  local cur prev cword
  cur=${COMP_WORDS[COMP_CWORD]}
  prev=${COMP_WORDS[COMP_CWORD-1]}
  cword=$COMP_CWORD
  if [[ $cur == -* ]]; then
    COMPREPLY=( $(compgen -W '--help -h' -- "$cur") )
    return
  fi
}
_dot_cmd_git_diff() {
  local cur prev cword
  cur=${COMP_WORDS[COMP_CWORD]}
  prev=${COMP_WORDS[COMP_CWORD-1]}
  cword=$COMP_CWORD
  case $prev in
    --tab)
      COMPREPLY=( $(compgen -W 'changed other unchanged' -- "$cur") )
      return
      ;;
  esac

  if [[ $cur == -* ]]; then
    COMPREPLY=( $(compgen -W '--no-fetch --tab --raw --bar-json --list-changed --list-all --help -h' -- "$cur") )
    return
  fi
}
_dot_cmd_git_commit() {
  local cur prev cword
  cur=${COMP_WORDS[COMP_CWORD]}
  prev=${COMP_WORDS[COMP_CWORD-1]}
  cword=$COMP_CWORD
  case $prev in
    --path)
      COMPREPLY=( $(compgen -f -- "$cur") )
      return
      ;;
  esac

  if [[ $cur == -* ]]; then
    COMPREPLY=( $(compgen -W '--message -m --path --amend --push --dry-run --help -h' -- "$cur") )
    return
  fi
}
_dot_cmd_git_log() {
  local cur prev cword
  cur=${COMP_WORDS[COMP_CWORD]}
  prev=${COMP_WORDS[COMP_CWORD-1]}
  cword=$COMP_CWORD
  if [[ $cur == -* ]]; then
    COMPREPLY=( $(compgen -W '--raw --help -h' -- "$cur") )
    return
  fi
}
_dot_cmd_git_workflows() {
  local cur prev cword
  cur=${COMP_WORDS[COMP_CWORD]}
  prev=${COMP_WORDS[COMP_CWORD-1]}
  cword=$COMP_CWORD
  if [[ $cur == -* ]]; then
    COMPREPLY=( $(compgen -W '--since --raw --bar-json --list-repos --list-runs --help -h' -- "$cur") )
    return
  fi
}
_dot_cmd_git_notifications() {
  local cur prev cword
  cur=${COMP_WORDS[COMP_CWORD]}
  prev=${COMP_WORDS[COMP_CWORD-1]}
  cword=$COMP_CWORD
  if [[ $cur == -* ]]; then
    COMPREPLY=( $(compgen -W '--raw --bar-json --list-threads --bar-filter --all --participating --since --mark-read --mark-bot-read --dry-run --mark-done --ignore --unignore --help -h' -- "$cur") )
    return
  fi
}
_dot_cmd_agents_sync() {
  local cur prev cword
  cur=${COMP_WORDS[COMP_CWORD]}
  prev=${COMP_WORDS[COMP_CWORD-1]}
  cword=$COMP_CWORD
  if [[ $cur == -* ]]; then
    COMPREPLY=( $(compgen -W '--help -h' -- "$cur") )
    return
  fi
}
_dot_cmd_mcp_sync() {
  local cur prev cword
  cur=${COMP_WORDS[COMP_CWORD]}
  prev=${COMP_WORDS[COMP_CWORD-1]}
  cword=$COMP_CWORD
  if [[ $cur == -* ]]; then
    COMPREPLY=( $(compgen -W '--help -h' -- "$cur") )
    return
  fi
}
_dot_cmd_notes_capture_sync() {
  local cur prev cword
  cur=${COMP_WORDS[COMP_CWORD]}
  prev=${COMP_WORDS[COMP_CWORD-1]}
  cword=$COMP_CWORD
  if [[ $cur == -* ]]; then
    COMPREPLY=( $(compgen -W '--help -h' -- "$cur") )
    return
  fi
}
_dot_cmd_is_agent() {
  local cur prev cword
  cur=${COMP_WORDS[COMP_CWORD]}
  prev=${COMP_WORDS[COMP_CWORD-1]}
  cword=$COMP_CWORD
  if [[ $cur == -* ]]; then
    COMPREPLY=( $(compgen -W '--quiet -q --json --help -h' -- "$cur") )
    return
  fi
}
_dot_cmd_setup_public_repo() {
  local cur prev cword
  cur=${COMP_WORDS[COMP_CWORD]}
  prev=${COMP_WORDS[COMP_CWORD-1]}
  cword=$COMP_CWORD
  if [[ $cur == -* ]]; then
    COMPREPLY=( $(compgen -W '--help -h' -- "$cur") )
    return
  fi
}
_dot_cmd_setup_private_repo() {
  local cur prev cword
  cur=${COMP_WORDS[COMP_CWORD]}
  prev=${COMP_WORDS[COMP_CWORD-1]}
  cword=$COMP_CWORD
  if [[ $cur == -* ]]; then
    COMPREPLY=( $(compgen -W '--help -h' -- "$cur") )
    return
  fi
}
_dot_cmd_private_pkg_publish() {
  local cur prev cword
  cur=${COMP_WORDS[COMP_CWORD]}
  prev=${COMP_WORDS[COMP_CWORD-1]}
  cword=$COMP_CWORD
  if [[ $cur == -* ]]; then
    COMPREPLY=( $(compgen -W '--no-git --skip-build --install --help -h' -- "$cur") )
    return
  fi
}
_dot_cmd_skill_updates() {
  local cur prev cword
  cur=${COMP_WORDS[COMP_CWORD]}
  prev=${COMP_WORDS[COMP_CWORD-1]}
  cword=$COMP_CWORD
  if [[ $cur == -* ]]; then
    COMPREPLY=( $(compgen -W '--check --update --json --skill --no-commit --skip-review --help -h' -- "$cur") )
    return
  fi
}
_dot_cmd_skill_check() {
  local cur prev cword
  cur=${COMP_WORDS[COMP_CWORD]}
  prev=${COMP_WORDS[COMP_CWORD-1]}
  cword=$COMP_CWORD
  if [[ $cur == -* ]]; then
    COMPREPLY=( $(compgen -W '--open-opencode --diff-origin --help -h' -- "$cur") )
    return
  fi
}
_dot_cmd_completions() {
  local cur prev cword
  cur=${COMP_WORDS[COMP_CWORD]}
  prev=${COMP_WORDS[COMP_CWORD-1]}
  cword=$COMP_CWORD
  if [[ $cur == -* ]]; then
    COMPREPLY=( $(compgen -W '--stdout --help -h' -- "$cur") )
    return
  fi
  if (( cword == 2 )); then
    COMPREPLY=( $(compgen -W 'bash fish zsh' -- "$cur") )
    return
  fi
}
_dot_cmd_omarchy() {
  local cur prev cword
  cur=${COMP_WORDS[COMP_CWORD]}
  prev=${COMP_WORDS[COMP_CWORD-1]}
  cword=$COMP_CWORD
  if [[ $cur == -* ]]; then
    COMPREPLY=( $(compgen -W '--help -h' -- "$cur") )
    return
  fi
  if (( cword >= 2 )); then
    COMPREPLY=( $(compgen -W 'theme font toggle capture system launch refresh restart install remove packages share reminder setup snapshot brightness power' -- "$cur") )
    return
  fi
}
_dot_cmd_usage() {
  local cur prev cword
  cur=${COMP_WORDS[COMP_CWORD]}
  prev=${COMP_WORDS[COMP_CWORD-1]}
  cword=$COMP_CWORD
  case $prev in
    --format)
      COMPREPLY=( $(compgen -W 'text json agent-context' -- "$cur") )
      return
      ;;
    --root)
      COMPREPLY=( $(compgen -f -- "$cur") )
      return
      ;;
  esac

  if [[ $cur == -* ]]; then
    COMPREPLY=( $(compgen -W '--days --format --root --history --apply --help -h' -- "$cur") )
    return
  fi
}
_dot_cmd_help() {
  local cur prev cword
  cur=${COMP_WORDS[COMP_CWORD]}
  prev=${COMP_WORDS[COMP_CWORD-1]}
  cword=$COMP_CWORD
  if [[ $cur == -* ]]; then
    COMPREPLY=( $(compgen -W '--help -h' -- "$cur") )
    return
  fi
}
_dot() {
  local cur cword command
  COMPREPLY=()
  cur=${COMP_WORDS[COMP_CWORD]}
  cword=$COMP_CWORD
  command=${COMP_WORDS[1]-}

  if (( cword == 1 )); then
    COMPREPLY=( $(compgen -W 'dashboard init install update up stow firewall doctor clean git-diff diff git-commit git-log git-workflows git-notifications agents-sync mcp-sync notes-capture-sync is-agent setup-public-repo setup-private-repo private-pkg-publish skill-updates skill-check completions omarchy usage help -h --help' -- "$cur") )
    return
  fi

  case $command in
    dashboard)
      _dot_cmd_dashboard
      ;;
    init)
      _dot_cmd_init
      ;;
    install)
      _dot_cmd_install
      ;;
    update|up)
      _dot_cmd_update
      ;;
    stow)
      _dot_cmd_stow
      ;;
    firewall)
      _dot_cmd_firewall
      ;;
    doctor)
      _dot_cmd_doctor
      ;;
    clean)
      _dot_cmd_clean
      ;;
    git-diff|diff)
      _dot_cmd_git_diff
      ;;
    git-commit)
      _dot_cmd_git_commit
      ;;
    git-log)
      _dot_cmd_git_log
      ;;
    git-workflows)
      _dot_cmd_git_workflows
      ;;
    git-notifications)
      _dot_cmd_git_notifications
      ;;
    agents-sync)
      _dot_cmd_agents_sync
      ;;
    mcp-sync)
      _dot_cmd_mcp_sync
      ;;
    notes-capture-sync)
      _dot_cmd_notes_capture_sync
      ;;
    is-agent)
      _dot_cmd_is_agent
      ;;
    setup-public-repo)
      _dot_cmd_setup_public_repo
      ;;
    setup-private-repo)
      _dot_cmd_setup_private_repo
      ;;
    private-pkg-publish)
      _dot_cmd_private_pkg_publish
      ;;
    skill-updates)
      _dot_cmd_skill_updates
      ;;
    skill-check)
      _dot_cmd_skill_check
      ;;
    completions)
      _dot_cmd_completions
      ;;
    omarchy)
      _dot_cmd_omarchy
      ;;
    usage)
      _dot_cmd_usage
      ;;
    help)
      _dot_cmd_help
      ;;
  esac
}

complete -F _dot dot

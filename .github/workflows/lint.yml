---
name: Lint

on:
  push:
    branches:
      - distro/arch-omarchy-quattro
  pull_request: {}
  workflow_dispatch: {}

permissions:
  contents: read

jobs:
  changes:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: read
    outputs:
      dot: ${{ steps.changes.outputs.dot }}
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          fetch-depth: 0

      - name: Detect dot changes
        id: changes
        if: github.event_name != 'workflow_dispatch'
        uses: dorny/paths-filter@ceb8a2b8f2d89434be7ff52d3de7ec3738c5cc9d # v4.0.3
        with:
          filters: |
            dot:
              - 'dot/**'
              - 'mise.toml'
              - '.github/actions/setup-mise/**'
              - 'package.json'
              - 'tests/dot/**'
              - '.github/workflows/dot-build.yml'
              - '.github/workflows/lint.yml'

  dot-typecheck:
    needs:
      - changes
    if: github.event_name == 'workflow_dispatch' || needs.changes.outputs.dot == 'true'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          submodules: true

      - name: Setup mise
        uses: ./.github/actions/setup-mise

      - name: Install dot dependencies
        working-directory: dot
        run: bun install --frozen-lockfile

      - name: Type check
        run: mise run dot:typecheck

  oxlint:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
        with:
          submodules: true

      - name: Setup mise
        uses: ./.github/actions/setup-mise

      - name: Install dot dependencies
        working-directory: dot
        run: bun install --frozen-lockfile

      - name: Install root dependencies
        run: bun install --frozen-lockfile

      - name: Install OpenCode v2 plugin dependencies
        run: bun install --cwd agents/.config/opencode/plugins-v2 --frozen-lockfile

      - name: Run TypeScript lint
        run: mise run lint

  validate-skills:
    uses: timmo001/workflows/.github/workflows/lint-agent-skills.yml@76f563af4f42ee9be7afe567514c4e376b3b56c6 # master
    with:
      skill-roots: .agents/skills dotfiles-skills/.agents/skills

  actionlint:
    uses: timmo001/workflows/.github/workflows/lint-actionlint.yml@76f563af4f42ee9be7afe567514c4e376b3b56c6 # master

  markdownlint:
    uses: timmo001/workflows/.github/workflows/lint-markdownlint.yml@76f563af4f42ee9be7afe567514c4e376b3b56c6 # master

  yamllint:
    uses: timmo001/workflows/.github/workflows/lint-yamllint.yml@76f563af4f42ee9be7afe567514c4e376b3b56c6 # master

  jsonlint:
    uses: timmo001/workflows/.github/workflows/lint-jsonlint.yml@76f563af4f42ee9be7afe567514c4e376b3b56c6 # master

  markdown-links:
    uses: timmo001/workflows/.github/workflows/lint-markdown-links.yml@76f563af4f42ee9be7afe567514c4e376b3b56c6 # master

  shellcheck:
    uses: timmo001/workflows/.github/workflows/lint-shellcheck.yml@76f563af4f42ee9be7afe567514c4e376b3b56c6 # master
    with:
      scandir: ./scripts/.local/bin
      severity: warning
      ignore_names: gh-import-dotenv-to-secrets

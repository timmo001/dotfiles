#!/bin/bash

set -euo pipefail

active_class="$(hyprctl activewindow -j | jq -r '.class // ""')"

if [[ "$active_class" == "com.mitchellh.ghostty" ]]; then
  exec hyprctl dispatch sendshortcut SUPER, W, activewindow
fi

exec hyprctl dispatch killactive

#!/usr/bin/env bash

set -euo pipefail

remote=origin
if git remote get-url upstream >/dev/null 2>&1; then
  remote=upstream
fi

branch=$(git branch --show-current)
echo "Rebasing ${branch:-HEAD} onto $remote's default branch..."

git fetch "$remote" HEAD
git rebase --autostash FETCH_HEAD

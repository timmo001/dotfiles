# Docs Agent Guide

This directory is the Astro + Starlight documentation site for the dotfiles repo, deployed to `dotfiles.timmo.dev`. Keep changes small and aligned with existing patterns.

## Toolchain

- Use **bun** only (`bun install`, `bun run dev`, `bun run build`, `bun run preview`).
- Node + bun versions are pinned in `mise.toml` for local dev. Vercel uses its own Node and detects bun via `bun.lock`.

## Layout

- Content: `src/content/docs/` (Markdown/MDX with YAML frontmatter; file names map to routes).
- Components: `src/components/` (`LandingCard.astro`).
- Styles: `src/styles/starlight.css`.
- Branding: `src/assets/logo.svg` + `public/favicon.svg` (twemoji toolbox).
- Nav, site config, and OG meta: `astro.config.mjs`.

## Generated content — do not hand-edit

Two areas are generated from the single source of truth in the repo:

- `src/content/docs/dot/commands.md` ← `dot/src/cli/spec.ts` via `bun run gen:cli`.
- `src/content/docs/reference/{agents,commands,skills,plugins}.md` ← `agents/.config/opencode/**` and `agents/.agents/skills/**` via `bun run gen:opencode`.

`bun run dev` regenerates both first (via `predev`). `bun run build` does **not** regenerate (Vercel builds the committed output). CI regenerates and commits these when their sources change. When you change a `dot` command or an OpenCode asset, run `bun run gen` and commit the result (the `dot` reference also pairs with shell completions).

The Open Graph image `public/og.png` is generated by `bun run og` from `src/assets/logo.svg`; regenerate it only when the branding or tagline changes.

## Authoring

- Use YAML frontmatter with `title` and `description`.
- File names are kebab-case and map to routes; internal links are root-relative with trailing slashes (`/git/status/`).
- External links get `target="_blank"` + `rel` automatically via `rehype-external-links`; no need to add them by hand in plain Markdown.
- Use Starlight callouts (`:::note`, `:::tip`, `:::caution`) and language tags on code fences.
- `bun run build` runs `starlight-links-validator`; broken internal links fail the build.

## Source of truth

This site is the canonical home for human documentation of the dotfiles repo. The top-level `README.md` links here rather than duplicating content. Update the docs when the documented behaviour changes.

---
title: Configuration
description: Environment options and private git configuration.
sidebar:
  label: Overview
  order: 1
---

import LinkCardGrid from '../../../components/LinkCardGrid.astro';

export const cards = [
  { title: 'Environment Variables', href: '/configuration/environment/', description: 'All DOT_*, DOTFILES_*, and OMARCHY_* options.' },
  { title: 'Private Git Config', href: '/configuration/private-git/', description: 'The dot-git.yml schema for repo activity, workflows, and notifications.' },
  { title: 'Private Dashboard Config', href: '/configuration/private-dashboard/', description: 'The dot-dashboard.yml schema for optional dashboard source cards.' },
  { title: 'Yazi', href: '/configuration/yazi/', description: 'Restore the shared Yazi plugin set across devices.' },
  { title: 'Plannotator', href: '/configuration/plannotator/', description: 'Restore shared Plannotator review settings across devices.' },
  { title: 'Lazygit', href: '/configuration/lazygit/', description: 'Run shared repository maintenance commands from Lazygit.' },
  { title: 'Cursor', href: '/configuration/cursor/', description: 'Restore the last window type or choose Editor and Agents explicitly.' },
  { title: 'Packages', href: '/configuration/packages/', description: 'Use the signed public repository, AUR fallback, and private packages.' },
];

Most behaviour is controlled by explicit CLI flags. Runtime configuration uses environment variables for process context, tracked application config files, and private YAML files for git/GitHub repo tracking and optional dashboard source cards.

<LinkCardGrid cards={cards} />

:::note[Configuration policy]
For dotfiles and system scripts, prefer explicit CLI flags over environment-variable toggles for runtime behaviour. Environment variables are used for standard process context (`HOME`, `PATH`, `XDG_*`), secrets, paths, and compatibility shims.
:::

---
import type { HTMLAttributes } from 'astro/types';

interface Props {
  title: string;
  class?: HTMLAttributes<'article'>['class'];
}

const { title, class: className } = Astro.props;
const hasIcon = Astro.slots.has('icon');
---

<article class={['dot-card sl-flex', className].filter(Boolean).join(' ')}>
  <p class="dot-card-title sl-flex">
    {hasIcon && (
      <span class="dot-card-icon" aria-hidden="true">
        <slot name="icon" />
      </span>
    )}
    <span set:html={title} />
  </p>
  <div class="dot-card-body"><slot /></div>
</article>

<style>
  @layer starlight.components {
    .dot-card {
      --dot-card-border: var(--sl-color-orange);
      --dot-card-bg: var(--sl-color-orange-low);
      border: 1px solid var(--sl-color-gray-5);
      background-color: var(--sl-color-black);
      padding: clamp(1rem, calc(0.125rem + 3vw), 2.5rem);
      flex-direction: column;
      gap: clamp(0.5rem, calc(0.125rem + 1vw), 1rem);
    }
    .dot-card:nth-child(4n + 2) {
      --dot-card-border: var(--sl-color-green);
      --dot-card-bg: var(--sl-color-green-low);
    }
    .dot-card:nth-child(4n + 3) {
      --dot-card-border: var(--sl-color-blue);
      --dot-card-bg: var(--sl-color-blue-low);
    }
    .dot-card:nth-child(4n + 4) {
      --dot-card-border: var(--sl-color-purple);
      --dot-card-bg: var(--sl-color-purple-low);
    }
    .dot-card-title {
      font-weight: 600;
      font-size: var(--sl-text-h4);
      color: var(--sl-color-white);
      line-height: var(--sl-line-height-headings);
      gap: 1rem;
      align-items: center;
    }
    .dot-card-icon {
      background-color: var(--sl-color-gray-6);
      padding: 0.2em;
      border-radius: 0.25rem;
      flex-shrink: 0;
      color: var(--dot-card-border);
      display: inline-flex;
      align-items: center;
      justify-content: center;
    }
    .dot-card-icon :global(svg) {
      width: 1.333em;
      height: 1.333em;
    }
    .dot-card-body {
      margin: 0;
      font-size: var(--sl-text-body);
    }
  }
</style>

---
description: Run the OpenCode agent benchmark
---

Load and apply the `agent-benchmark` skill with these benchmark flags:
`${ARGUMENTS}`. Launch its experimental background task on every invocation.
